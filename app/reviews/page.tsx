import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { Card, CardContent } from "@/components/ui/card";
import { supportReview } from "@/lib/sample-data";
export default function ReviewsPage() { return <div className="space-y-7"><PageHeader eyebrow="Support Refund Agent · Sample" title="Stakeholder reviews" description="Keep each accountable assessment visible. Submission means a position is recorded; it does not imply consensus."/><div className="grid gap-4 lg:grid-cols-2">{supportReview.reviews.map(r => <Card key={r.role}><CardContent className="space-y-3 pt-5"><div className="flex items-center justify-between"><div><h2 className="font-semibold">{r.role}</h2><p className="text-xs text-slate-500">{r.reviewer}</p></div><StatusBadge status={r.recommendation}/></div><p className="text-sm leading-6 text-slate-600">{r.rationale}</p></CardContent></Card>)}</div><div className="rounded-lg border border-amber-200 bg-amber-50 p-4"><h2 className="font-semibold text-amber-950">Material disagreement remains visible</h2><p className="mt-1 text-sm text-amber-900">Product recommends Ship while engineering, trust/safety, and operations do not. The blocking gate and critical failure still determine the system recommendation.</p></div></div>; }
