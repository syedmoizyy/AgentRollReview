import Link from "next/link";
import { notFound } from "next/navigation";
import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { supportReview, workflows } from "@/lib/sample-data";
export default async function WorkflowPage({ params }: { params: Promise<{ slug: string }> }) { const { slug } = await params; const workflow = workflows.find(w => w.slug === slug); if (!workflow) notFound(); return <div className="space-y-7"><PageHeader eyebrow="Sample workflow" title={workflow.name} description={workflow.value} actions={<><Button variant="outline" asChild><Link href="/evaluations">Evidence</Link></Button><Button asChild><Link href="/decisions">Decision memo</Link></Button></>}/><div className="grid gap-4 md:grid-cols-4"><Card><CardContent className="pt-5"><p className="text-xs font-bold uppercase text-slate-500">Recommendation</p><div className="mt-3"><StatusBadge status={workflow.decision}/></div></CardContent></Card><Card><CardContent className="pt-5"><p className="text-xs font-bold uppercase text-slate-500">Task success</p><p className="mt-2 text-2xl font-semibold">{workflow.successRate}%</p></CardContent></Card><Card><CardContent className="pt-5"><p className="text-xs font-bold uppercase text-slate-500">Recovery</p><p className="mt-2 text-2xl font-semibold">{workflow.recoveryRate}%</p></CardContent></Card><Card><CardContent className="pt-5"><p className="text-xs font-bold uppercase text-slate-500">Risk</p><p className="mt-2 text-2xl font-semibold">{workflow.risk}</p></CardContent></Card></div><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>Profile context</CardTitle></CardHeader><CardContent className="space-y-4 text-sm"><div><strong>Owner</strong><p className="text-slate-600">{workflow.owner}</p></div><div><strong>Affected users</strong><p className="text-slate-600">{workflow.affectedUsers.join(", ")}</p></div><div><strong>Tools</strong><p className="text-slate-600">{workflow.tools.join(", ")}</p></div></CardContent></Card><Card><CardHeader><CardTitle>What needs attention</CardTitle></CardHeader><CardContent className="space-y-3">{slug === "support-refund-agent" ? supportReview.gates.filter(g => g.result === "Fail").map(g => <div key={g.name} className="flex items-center justify-between rounded-lg border border-red-100 bg-red-50 p-3 text-sm"><span>{g.name}</span><StatusBadge status="Fail"/></div>) : <p className="text-sm text-slate-600">Evidence and required reviews are incomplete. Investigate before disposition.</p>}</CardContent></Card></div></div>; }
