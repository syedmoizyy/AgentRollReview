import { PageHeader } from "@/components/page-header";
import { StatusBadge } from "@/components/status-badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { sampleDecision, supportReview } from "@/lib/sample-data";
export default function DecisionsPage() { return <div className="space-y-7"><PageHeader eyebrow="Decision memo · Sample" title="Block the Support Refund Agent" description="The deterministic recommendation is Block. High aggregate task success does not override failed blocking gates or an unresolved critical failure."/><Card className="border-red-200 bg-red-50"><CardContent className="flex flex-col gap-4 pt-5 md:flex-row md:items-center md:justify-between"><div><p className="text-xs font-bold uppercase tracking-wide text-red-700">System recommendation</p><p className="mt-1 text-2xl font-semibold text-red-950">{sampleDecision.outcome}</p></div><StatusBadge status="Block"/></CardContent></Card><div className="grid gap-4 lg:grid-cols-2"><Card><CardHeader><CardTitle>Reason codes</CardTitle></CardHeader><CardContent><ul className="space-y-2">{sampleDecision.reasonCodes.map(code => <li className="rounded bg-slate-100 p-2 font-mono text-xs" key={code}>{code}</li>)}</ul></CardContent></Card><Card><CardHeader><CardTitle>Unresolved risks and owners</CardTitle></CardHeader><CardContent className="space-y-3 text-sm"><div><strong>Tool recovery under partial API failure</strong><p className="text-slate-600">Owner: Platform team · Due date is a sample placeholder</p></div><div><strong>Unsafe refund outcome in 2 sample runs</strong><p className="text-slate-600">Owner: Trust & safety · Requires targeted rerun</p></div></CardContent></Card></div><Card><CardHeader><CardTitle>Evidence summary</CardTitle></CardHeader><CardContent className="space-y-2 text-sm"><p>Task success: 91 / 100 sample runs (91%).</p><p>Recovery success: 5 / 12 sample recovery attempts (42%), below the assumed 80% blocking threshold.</p><p>Four required reviews submitted; recommendations conflict and remain separately attributed.</p></CardContent></Card><Card><CardHeader><CardTitle>Audit log</CardTitle></CardHeader><CardContent><ol className="space-y-3 text-sm">{supportReview.audit.map((event, i) => <li className="flex gap-3" key={event}><span className="text-slate-400">0{i+1}</span><span>{event}</span></li>)}</ol></CardContent></Card></div>; }
